# 인형뽑기 (Claw Machine) — 스펙 (SDD)

바이브 코딩의 기준 문서. 각 요구사항은 ID를 가지며, **Phase 완료 = 해당 수용 기준(AC) 전부 통과**.
검증 방식: `[T]` = vitest 단위 테스트, `[C]` = 브라우저 플레이스루 체크리스트.

## 진행 현황

| Phase | 스펙 | 상태 |
|---|---|---|
| 0. 스켈레톤 | R6 준비 | ✅ |
| 1. 카탈로그 + 구덩이 | R1 | ✅ |
| 2. 클로 이동 (2버튼) | R2 | ✅ |
| 3. 잡기 | R3 | ✅ |
| 4. 게임화 + HUD | R4 | ✅ |
| 5. 운영자 모드 | R5 | ✅ |
| 6. 폴리시 + 검증 | R6 | ✅ |

---

## R1. 기계와 인형 구덩이

- [x] **R1.1** `[C]` 캐비닛(바닥·4벽·배출구 개구부)이 렌더되고, 인형이 벽을 뚫고 나갈 수 없다
- [x] **R1.2** `[T][C]` 카탈로그의 모든 인형 종류(≥4종)가 서로 다른 외형으로 렌더된다
- [x] **R1.3** `[C]` 인형 20개 낙하 후 5초 내 더미가 안정(슬립)되고, 지터가 없다 (60fps 근처 유지)

## R2. 클로 조작 (정통 2버튼)

- [x] **R2.1** `[T][C]` 버튼①을 누르는 동안 클로가 +X로 이동, 릴리스 시 정지 (경계 클램프). 재홀드 허용(관대 정책 — 여러 번 눌러도 됨)
- [x] **R2.2** `[T][C]` 이후 버튼②를 누르는 동안 +Z로 이동, 릴리스 시 **자동 하강** 시작. 재홀드 허용
- [x] **R2.3** `[T][C]` 클로는 최저점에서 손가락을 닫고 상승→원위치 복귀→개방까지 자동 진행
- [x] **R2.4** `[C]` 키보드와 화면 터치 버튼이 동일하게 동작한다 — 터치 버튼은 `onPointerDown/Up/Cancel/Leave` 기반이라 마우스 클릭으로도 동일 경로 검증됨(포인터 이벤트는 마우스·터치 공통). **모바일 실기기/좁은 뷰포트 시각 확인은 자동화 환경의 창 크기 제약으로 미실시** — 대신 좁은 화면에서 실제로 겹칠 수 있던 지점(디버그 패널↔안내 문구, 운영자 패널 버튼 줄바꿈)을 코드 검토로 찾아 반응형 CSS로 방어 처리(`index.html` 미디어쿼리, `OperatorPanel.tsx` flexWrap). 실기기 최종 확인 권장
- [x] **R2.5** `[T]` 라운드 시작 후 `roundTimeLimit`(20초) 초과 시 버튼②를 누르지 않아도 자동으로 하강 단계로 전이한다 (테스트로 검증, `stateMachine.test.ts`)

## R3. 잡기 판정

- [x] **R3.1** `[T][C]` 손가락이 닫힐 때 손바닥 기준 반경(`grabRadius`) 내 최근접 인형 1개가 후보가 된다 — 순수 함수 `pickNearest`(`logic/grab.ts`), 반경 밖/빈 배열은 null
- [x] **R3.2** `[T]` 성공 확률은 `gripStrength`를 따른다 (RNG 주입식 — `logic/grab.ts`)
- [x] **R3.3** `[C]` 성공 시 인형이 조인트로 매달려 흔들리며 운반된다 (LIFTING까지 실동작 확인 — 흰 토끼가 클로와 함께 들림)
- [x] **R3.4** `[T]` 확률적 "중간 낙하"가 발생할 수 있다 (시점 결정 순수 함수 — `logic/grab.ts`)
- [x] **R3.5** `[C]` 배출구 상공에서 릴리스된 인형이 배출구 센서를 통과하면 승리로 기록된다 — 해당 인형은 제거되고, 카탈로그의 `score`만큼 누적 점수에 가산된다. Phase 6에서 gripStrength=1로 전체 라운드(IDLE→...→RESULT→IDLE) 완주 확인, 점수 0→15(토끼) 반영 확인

## R4. 게임 루프·HUD

- [x] **R4.1** `[C]` 크레딧 차감(시작 10, 판당 1) → 1판 진행 → 결과 토스트 → IDLE 복귀가 무한 반복 가능하다. 크레딧 0이면 새 라운드 시작 불가, HUD "코인 채우기" 버튼으로 리필 — 전체 라운드 완주(Phase 6)로 차감→진행→IDLE 복귀 확인
- [x] **R4.2** `[C]` 최고 기록(누적 점수)이 localStorage에 저장·복원된다 — 새로고침 후 best 값 유지 확인됨
- [x] **R4.3** `[C]` 디버그 패널에서 gripStrength를 실시간 조절할 수 있다 — 슬라이더 조작이 store에 즉시 반영됨을 확인
- [x] **R4.4** `[C]` OrbitControls는 기계 정면 프레이밍을 벗어나지 않는다 (각도 제한, 팬 금지) — App.tsx에 각도/거리 제한 적용, 스크린샷으로 프레이밍 확인

## R5. 운영자 모드

- [x] **R5.1** `[C]` PLAY↔OPERATOR 토글은 `clawPhase === 'IDLE'`일 때만 허용되며, 전환 시 클로가 정지하고 운영 패널이 표시된다 — 토글·패널 표시·복귀 실동작 확인
- [x] **R5.2** `[T][C]` 팔레트에서 인형 종류 선택 후 기계 내부 클릭 → `clampSpawnXZ`로 내부 좌표로 클램프, 배출구 구역이면 배치 거부 → 해당 지점에 스폰(낙하 안착) — 클릭 지점에 곰돌이 스폰·안착 실동작 확인 (20→21)
- [x] **R5.3** `[C]` 인형 클릭으로 삭제할 수 있다 — 실동작 확인 (20→19)
- [x] **R5.4** `[C]` "비우기 / 랜덤 채우기" 버튼이 동작한다 — 둘 다 실동작 확인 (20→0, 0→20)
- [x] **R5.5** `[T][C]` 현재 레이아웃(타입·위치·회전)을 저장하고, 재시작 후 복원할 수 있다 — 저장(실제 정착 pos/quat)→비우기→불러오기로 동일 배치 복원 실동작 확인

## R6. 품질 게이트

- [x] **R6.1** `npm test`: `logic/`·`catalog.ts` 테스트 전부 통과 — 42개 전부 통과 (stateMachine·grab·layout·catalog)
- [x] **R6.2** `npm run build` 무경고 통과 (TS strict) — 통과. 번들 크기 경고(3.3MB, rapier WASM base64 내장분)만 있음 — 로컬/교육용 무해, 배포 시에만 `chunkSizeWarningLimit` 조정 검토
- [x] **R6.3** 물리·렌더링 AC는 브라우저 플레이스루로 검증 — R1~R5 전 항목 실동작 확인 완료 (본 문서 각 항목 참조)

---

## 아키텍처 규칙 (구현 시 준수)

1. **`src/logic/`은 Three/Rapier 임포트 금지** — 순수 함수만, 전부 테스트 대상
2. **매직넘버는 전부 `src/config.ts`** — 튜닝 세션은 단일 파일 수정
3. 클로는 `kinematicPosition` 바디, 이동은 반드시 `setNextKinematicTranslation()` (속도 클램프)
4. 컨테인먼트는 두꺼운(≥0.15) 투명 CuboidCollider — 얇은/트라이메시 콜라이더 금지
5. 타이머는 timestamp 기반 (`setTimeout` 금지)
6. 게임 상태: `IDLE → MOVING_X → MOVING_Z → DROPPING → GRABBING → LIFTING → RETURNING → RELEASING → RESULT → IDLE`
7. 라이선스: 의존성·에셋은 MIT/Apache-2.0/CC0만
